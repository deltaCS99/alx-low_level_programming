#include "main.h"
#include <string.h>

/**
 * _strncpy - check the code
 * @dest: string
 * @src: string
 * @n: n
 *
 * Return: string copy
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
