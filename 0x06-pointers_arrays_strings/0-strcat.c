#include "main.h"
#include <string.h>

/**
 * _strcat - check the code
 *
 * @dest: destination
 * @src: source
 *
 * Return: merged string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
